// createBid(driverId, jobId, price, message)
// getBidsForJob(jobId)
// getBidsByDriver(driverId)
// updateBid(bidId, newData)
// deleteBid(bidId)
// markBidAsAccepted(bidId) — maybe tied to acceptBid in user-actions
